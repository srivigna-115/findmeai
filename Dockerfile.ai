FROM python:3.11-slim

WORKDIR /app

COPY ai-service/requirements.txt .

RUN pip install --no-cache-dir -r requirements.txt

COPY ai-service/ .

EXPOSE 5001

CMD ["gunicorn", "--bind", "0.0.0.0:$PORT", "--workers", "2", "--timeout", "120", "app_simple:app"]
